# Simple Go Library Example

This directory contains a self-contained example of embedding ProtoDocs into an existing Go application.

## How to Run

1. Run the main server:
   ```sh
   go run main.go
   ```

2. Open your browser and navigate to:
   [http://localhost:8080/docs/](http://localhost:8080/docs/)
